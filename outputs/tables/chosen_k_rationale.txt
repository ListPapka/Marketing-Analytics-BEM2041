Minimum cluster size constraint: >= 50 respondents per cluster (max(5, 5% of n)). Selected k=2 with silhouette=0.2668, CH=395.60, DB=1.4771.

Interpretability: solutions with fewer clusters are typically easier to act on (messaging, product priorities); higher k may improve statistical fit but can produce thin segments — check minimum cluster sizes in cluster_metrics_by_k.csv.
